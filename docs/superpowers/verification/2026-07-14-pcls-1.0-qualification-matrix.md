# PCLS 1.0 qualification matrix - 2026-07-14

## Release decision

**Blocked.** The code-level and disposable-filesystem qualification passed, but the host does not provide Playnite Desktop, Playnite Fullscreen, `Toolbox.exe`, an rclone executable, or validated removable/mapped/UNC targets. Installed and real-provider rows therefore remain blocked. PCLS must remain at the pre-release version `0.3.2`; this document does not authorize a 1.0 identity, release publication, or PR merge. Task 17 must not begin until this Task 16 matrix has zero Blocked and zero Failed rows.

No external software was installed or downloaded. No GUI was launched. No real Playnite profile, library, settings, cache, or configured rclone remote was mutated. The rclone inventory records availability/count only and intentionally omits remote names and configuration contents.

## Host prerequisite inventory

| Prerequisite | Status | Evidence | Exact unblock action |
| --- | --- | --- | --- |
| Isolated release branch | Passed | Linked worktree at the current repository on `feature/user-friendly-dashboard-cleanup`; `git rev-parse --git-dir` points to `.git/worktrees/pcls-branding-worktree`. | None. |
| Playnite Desktop | Blocked | Not on `PATH`; no executable under the standard Program Files locations or `%LOCALAPPDATA%\Playnite`, which contains only `User Data`. | Install or supply the intended supported Playnite build, record its version/path, and run the installed Desktop matrix against a disposable Playnite profile. |
| Playnite Fullscreen | Blocked | No `Playnite.FullscreenApp.exe` found with the Desktop inventory. | Supply the same qualified Playnite installation and run the Fullscreen matrix against a disposable profile. |
| Official Playnite Toolbox | Blocked | Not on `PATH`, `PLAYNITE_TOOLBOX` unset, and absent from documented installed locations. The real validator returned exit 2 with `PREREQUISITE_MISSING`. | Supply the official `Toolbox.exe` through `-ToolboxPath` or `PLAYNITE_TOOLBOX`, then rerun `tools/validate-release.ps1`. |
| rclone | Blocked | A config file is present and contains one configured remote, but no `rclone.exe` is on `PATH`; no config contents or remote name were recorded. | Supply the intended rclone executable, designate a read-only probe and a disposable writable target, then run provider and transfer qualification. |
| Removable/external target | Blocked | Only local C: and D: filesystem drives were visible; no target was verified as removable. Hardware classification through WMI was unavailable. | Attach and explicitly designate a disposable removable target, verify its drive type, then run import/install/cancel/cleanup/uninstall cases. |
| Mapped drive | Blocked | No mapped SMB drive was reported by `Get-SmbMapping` or filesystem drive inventory. | Map and explicitly designate a disposable share, then run the provider matrix. |
| UNC/NAS target | Blocked | No disposable UNC/NAS endpoint was supplied or discoverable without probing private configuration. | Supply an authorized disposable UNC path and run the provider matrix without touching production NAS content. |
| Historical upgrade inputs | Blocked | Repository fixtures `settings-v0.1.1.yaml` and `settings-v0.2.0.yaml` exist; no installed historical PCLS packages/profile snapshots were available. | Supply authentic 0.1.1 and 0.2.0 packages plus disposable Playnite profile snapshots for installed upgrade qualification. |

## Fresh automated evidence

Commands ran from the linked worktree on 2026-07-14 (America/New_York).

| ID | Command/evidence | Result |
| --- | --- | --- |
| A1 | MSBuild `PersonalCloudLibrarySource.Tests.csproj /t:Rebuild /p:Configuration=Debug /v:minimal /nologo` | Passed, exit 0; production and test assemblies built. |
| A2 | `PersonalCloudLibrarySource.Tests.exe --labels=Off` | Passed, 316/316; 0 failed, warnings, inconclusive, or skipped. The suite uses GUID-scoped temporary roots for destructive filesystem cases. |
| A3 | MSBuild `PersonalCloudLibrarySource.Tests.csproj /t:Rebuild /p:Configuration=Release /v:minimal /nologo` | Passed, exit 0. |
| A4 | `tools/package-extension.ps1` | Passed; Release solution build had 0 warnings/errors and produced `dist/PersonalCloudLibrarySource-0.3.2.pext`. |
| A5 | `tools/test-release-baseline.ps1 -PackagePath dist/PersonalCloudLibrarySource-0.3.2.pext` | Passed for manifest-derived version 0.3.2. |
| A6 | Direct disposable package expansion | Passed; exact files were `PersonalCloudLibrarySource.dll`, `extension.yaml`, `icon.png`, `Localization/en_US.xaml`, and `Assets/pcls-logo-wide.png`. Package size 596,052 bytes; SHA-256 `991D21D4EA308C90EDFD63687893141A07DB5689E72A60085B0D43DB03B04395`. |
| A7 | `tools/test-release-validation.ps1` | Passed, 6/6 structural/official-command contract cases. |
| A8 | `tools/validate-release.ps1 -PackagePath dist/PersonalCloudLibrarySource-0.3.2.pext` | Blocked as designed, exit 2: `PREREQUISITE_MISSING` because official Toolbox was not found. No release input was mutated. |
| A9 | `git diff --check` and tracked-status inspection after qualification | Passed; no whitespace errors and no unintended tracked build/package changes before this evidence document was added. |

## Required qualification matrix

`Passed` means the stated row was actually executed at the scope named in the row. A code-level pass is not substituted for an installed/manual pass.

| Area | Scope | Status | Exact evidence | Remaining action when blocked |
| --- | --- | --- | --- | --- |
| Clean install | Desktop `.pext` installation and first launch | Blocked | Playnite Desktop unavailable; no installation was attempted. | Install the freshly produced package into a disposable clean Playnite profile; record extension discovery, first launch, and absence of startup errors. |
| First-run setup | Installed setup wizard, save/cancel/reopen | Blocked | Host UI unavailable. Automated coverage passed in A2: `SetupWizardViewModelTests`, `SetupCompletionCoordinatorTests`, `SetupStatePersistenceServiceTests`, `SetupLaunchPolicyServiceTests`, and `SetupWizardEditLifecycleContractTests`. | Execute the wizard manually in Desktop and capture settings persistence and restart evidence. |
| Upgrade 0.1.1 | Installed upgrade with settings/data preservation | Blocked | No 0.1.1 package/profile. A2 passed `LoadAndMigrate_Deserialized011Fixture_PreservesPathsProviderAndSafetyChoices` and sequential/idempotent migration tests. | Install 0.1.1 in a disposable profile, populate representative data/settings, upgrade to candidate package, then compare settings, imported games, cache metadata, and logs. |
| Upgrade 0.2.0 | Installed upgrade with settings/data preservation | Blocked | No 0.2.0 package/profile. A2 passed `LoadAndMigrate_Deserialized020Fixture_PreservesGenerationStateAndUpgradesOldTimeoutDefault` and preference-preservation tests. | Repeat the installed preservation comparison from an authentic 0.2.0 profile. |
| LocalFile provider | Code-level import/path/state behavior with disposable files | Passed | A2: `ManifestLoaderTests`, `ManifestParserValidatorTests`, `ImportOutcomeServiceTests`, `PathResolverTests`, and `LibraryItemStateResolverTests`. | Installed Desktop/Fullscreen provider smoke is still covered by their blocked UI rows. |
| LocalFolder provider | Code-level generation/import/directory transfer with disposable folders | Passed | A2: `ManifestItemMapperTests`, `ManifestGenerationCancellationTests`, `LocalTransferAdapterTests.CopyDirectory_*`, and directory state resolver cases. | Installed provider smoke remains required. |
| External drive provider | Actual removable-drive availability and I/O | Blocked | No verified removable target. Local path-policy tests passing in A2 do not prove device behavior. | Run the full provider/transfer/unplug/reconnect matrix on an explicitly designated disposable removable drive. |
| Mapped drive provider | Actual mapped-drive availability and I/O | Blocked | No mapped drive. Path syntax coverage is not an installed provider run. | Run import, install, disconnect/reconnect, error recovery, and safe cleanup on a disposable mapping. |
| UNC/NAS provider | Actual UNC/NAS availability and I/O | Blocked | No authorized disposable UNC endpoint. | Run the same matrix directly against an authorized disposable UNC path, including temporary unavailability. |
| RcloneRemote provider | Actual configured provider probe/import/download | Blocked | Config availability/count confirmed, but executable absent; no remote was contacted. A2 passed fake-runner command, timeout, progress, cancellation, and adapter contracts only. | Supply rclone plus authorized read-only and disposable writable locations; run `listremotes`, manifest read, file/directory transfer, cancellation, cleanup, and recovery. |
| Install | Queued local file/directory install logic | Passed | A2: `CloudTransferExecutorTests`, `LocalTransferAdapterTests`, `GameWorkflowControllerTests.Install_TransferFailure_NotifiesWithoutPlayniteApiOrDesktopDialog`, and shared-state wiring contracts. | Installed command invocation remains part of blocked Desktop/Fullscreen rows. |
| Real concurrency | Concurrent worker enforcement | Passed | A2: `TransferQueueServiceTests.Workers_EnforceConfiguredConcurrencyAndObserveAllJobs` and `SetMaxConcurrentTransfers_IncreaseStartsAnotherWorkerAndDecreaseAppliesToNextClaim`; these execute concurrent workers, not a static mock assertion. | Real-provider throughput remains blocked by provider prerequisites. |
| Progress | Progress parsing/publication and queue presentation | Passed | A2: `RcloneProgressParserTests`, `CloudTransferManagerTests`, `CloudTransferQueueItemViewModelTests`, and dashboard queue snapshot tests. | Visually confirm installed progress presentation in Desktop/Fullscreen. |
| Cancellation | Queued/active/verification/finalization cancellation | Passed | A2: `TransferQueueServiceTests.Cancel_*`, `CancelDuringVerification_CancelsOnceAndLeavesNoPartialOrFinalData`, `CancelBetweenVerificationCheckAndCommit_IsAcceptedAndLeavesNoData`, local adapter cancellation tests, and fake rclone process cancellation tests. | Repeat against real rclone and installed UI when prerequisites exist. |
| Retry | Retry ownership and duplicate suppression | Passed | A2: `TransferQueueServiceTests.Retry_SuppressesDuplicateActiveAttemptAndIsQueueOwned`, `DashboardTransferActionsTests`, and runtime settings snapshot retry contract. | Confirm user-facing installed retry command in both host modes. |
| Shutdown | Bounded cancellation/disposal and no new work | Passed | A2: `TransferQueueServiceTests.Shutdown_IsBoundedIdempotentCancelsActiveAndRejectsNewWork`, `StartupActionServiceTests`, `StartupUiDispatcherTests`, and startup wiring contracts. | Run controlled Playnite shutdown during a real local and rclone transfer. |
| Verification | Destination/state/manifest diagnostics | Passed | A2: `LocalTransferAdapterTests`, `RcloneTransferAdapterTests`, `LibraryVerificationManifestDiagnosticsTests`, `VerificationDashboardStateServiceTests`, and manifest validator tests. | Installed status reconciliation remains part of blocked UI/provider rows. |
| Verification | Installed same-process source change and plugin reload | Blocked | Playnite Desktop unavailable, so `VerificationDashboardStateService.LatestReport` was not exercised across repeated verification, source changes, or plugin reload in one host process. Automated service tests do not prove that a stale same-source report/state cannot leak into the newly selected source or a reloaded plugin instance. | In a disposable Playnite profile, verify source A and record `LatestReport`; switch to source B in the same Desktop process and confirm the dashboard/report shows only B; switch back and reverify; disable/re-enable or reload PCLS without restarting Playnite; confirm no prior report/state survives into a different source or fresh plugin instance; repeat once after an intentional verification failure and once after recovery, retaining screenshots and logs. |
| Partial cleanup | Disposable partial removal and cleanup-failure semantics | Passed | A2: local/rclone adapter partial-path tests, `TransferPartialPathPolicyTests`, cancellation cleanup cases, and `CancellationCleanupFailure_RemainsFailedInsteadOfCancelled`. | Repeat on real removable/mapped/UNC/rclone targets. |
| Safe uninstall | Policy and executor behavior in disposable roots | Passed | A2: `SafeCacheDeletionPolicyTests`, `GameWorkflowPolicyServiceTests.Evaluate_UnsafeOutsideCacheTarget_RefusesUninstall`, controller authorized/refusal cases, and `PathStateConsumerContractTests.UninstallController_DelegatesDeletionToSafeExecutor`. | Manually verify installed confirmation/refusal/success without touching real library data. |
| Desktop dashboard | View-model status/activity/queue behavior | Passed | A2: all 39 `Dashboard` tests, including exactly-once terminal activity, capped newest-first history, cancel/retry actions, coalescing, and disposal/reopen lifetime. | This is a code-level pass only; installed visual/interaction row below remains blocked. |
| Desktop dashboard | Installed visual and interaction smoke | Blocked | Playnite Desktop unavailable; no dashboard window was opened. | Open dashboard repeatedly, exercise status/activity/queue/cancel/retry, close/reopen, and capture screenshots/logs in a disposable profile. |
| Fullscreen workflows | Import/state/play/install/cancel/uninstall/error controller behavior | Passed | A2: `FullscreenWorkflowContractTests.StandardControllers_DoNotDependOnDesktopWindowsOrNavigation`, `GameWorkflowPolicyServiceTests`, `GameWorkflowControllerTests`, `GameWorkflowNotificationServiceTests`, import outcome/notification tests, and state resolver tests. | This is code-level evidence, not an installed host run. |
| Fullscreen workflows | Installed Fullscreen import/state/play/install/cancel/uninstall/error feedback | Blocked | Fullscreen executable unavailable; no manual interaction occurred. | In a disposable Fullscreen profile, import remote metadata and confirm uninstalled state; install through the standard game workflow; observe active transfer/cancel and a forced transfer failure notification; confirm cached metadata and play/working-directory behavior; uninstall an authorized cached target; refuse an unsafe outside-cache target; exercise `AskEachTime` and confirm it fails closed when no Desktop confirmation dialog is available; verify every error uses standard Fullscreen-compatible notifications and no custom dashboard, setup, settings, details, navigation, or other Desktop-only surface opens. Retain screenshots and logs for each case. |
| Localization | Resource resolution, formatting, sinks, and dialog labels | Passed | A2: all 9 `LocalizationContractTests`; no missing/duplicate resources or placeholder mismatches. | Installed scaling, clipping, and readability remain blocked in the next row and are required before release. |
| Localization | Installed high-DPI scaling, clipping, and readability | Blocked | No Playnite Desktop/Fullscreen UI was available, so resource correctness was not visually qualified under host themes or display scaling. | In disposable Desktop and Fullscreen profiles, inspect setup, settings, dashboard/status/activity/queue, game actions, notifications, diagnostics, and reports at 100%, 125%, 150%, and 200% Windows scaling; verify no localized text, controls, icons, or branding are clipped/overlapped, keyboard access remains usable, and foreground/background contrast and message readability are acceptable. Capture DPI-tagged screenshots. |
| Branding | Runtime/docs artwork wiring, dimensions, alpha, decode | Passed | A2: `UiContractTests.BrandArtwork_*`, `RuntimeBrandArtwork_HasExpectedDimensionsAndTransparency`, and `DocumentationBrandArtwork_DecodesWithTransparency`; A6 confirms both runtime assets are packaged. | Installed visual rendering remains blocked with host UI. |
| Screenshots | Repository image decode/link integrity | Passed | A2: `LiveDocumentationContractTests.ReferencedRepositoryImagesDecode` and `LiveMarkdown_LocalLinksAndImagesResolve`. | None for file integrity. |
| Screenshots | Candidate installed Desktop/Fullscreen screenshots | Blocked | No host UI run; repository screenshots cannot prove the candidate's installed rendering. | Capture candidate setup/settings/dashboard and Fullscreen workflow screens after installed qualification at 100%, 125%, 150%, and 200% Windows scaling; tag each screenshot with host mode/theme/DPI and check cropping, scaling, clipping, readability, theme contrast, and branding fidelity. |
| Diagnostics/reports | Structured import/verification/transfer errors | Passed | A2: `ImportOutcomeServiceTests`, `ImportDashboardStateServiceTests`, `LibraryVerificationManifestDiagnosticsTests`, notification tests, and transfer failure/cleanup tests. | Confirm reports and notification actions in installed host modes. |
| Documentation links | Local links/images, live guidance, external check record | Passed | A2: `LiveDocumentationContractTests`, including local link/image resolution, preparation status, rclone deadline guidance, distribution identity, and recorded external URL checks. | Re-run external URLs at release time if policy requires current network evidence. |
| Package structure | Candidate `.pext` identity and exact content | Passed | A4-A6; exact five-file package, manifest-derived 0.3.2 identity, SHA-256 recorded above. | Rebuild and re-hash after any later source change. |
| Metadata/YAML | Structural release surfaces and installer history | Passed | A2 `ReleaseValidationContractTests`; A5 and A7 passed malformed YAML, identity, URL/version, filename, and exact-content contracts. | Official Toolbox verification still blocked below. |
| Official Toolbox | `pack`, `verify addon`, `verify installer` | Blocked | A8 exit 2 with explicit missing-prerequisite message. Contract tests cannot substitute for official execution. | Supply official Toolbox and rerun the validator to exit 0; retain its output and official package hash. |

## Defects and release stop

No code defect was discovered by this qualification run, so no production or test code was changed. The release remains stopped solely because required installed/manual, real-provider/hardware, and official Toolbox rows are blocked. After prerequisites are supplied, rerun every blocked row and update this matrix with dated evidence; do not convert a row to Passed based only on existing unit tests. Task 17 must not begin until every row in this Task 16 matrix is Passed, with zero Blocked and zero Failed rows.
